# 숏폼 자동화 생성기 PRD 초안

> 2026-09-08 공급자 변경: Creatify API의 고정 구독 비용을 이유로 미디어 생성 공급자를 Gemini API로 전환했다. P0 생성 흐름은 `OpenAI 대본 → Nano Banana 장면 이미지 및 승인 → Gemini TTS 한국어 음성 및 승인 → Gemini Omni 영상 및 승인 → FFmpeg 합성`이다. 기존 문서에서 Creatify·아바타·립싱크를 전제로 한 항목은 이 결정으로 대체하며, 정확한 말하는 아바타와 입 모양 동기화는 제외 범위로 이동한다. Gemini 계정과 사용료는 클라이언트가 소유·직접 결제한다. 모델명과 사용량 요율은 운영 설정으로 관리하고 실제 생성 전 요율 검토일과 원화 환산율을 기록한다.

- 버전: v0.19 / 수정일: 2026-09-05
- 상태: D01~D15 및 D06a~D06c 확정. 주요 제품 의사 결정 반영 완료. 구현 전 공급자 API 검증과 세부 설계가 필요하다.
- 참고 문서: `C:\Users\user\Downloads\creatify-api-system-blueprint.md`
- 현재 작업 범위: 2026-09-05 이후 사용자의 ‘P0 구현’ 요청에 따라 독립 폴더 `C:\Users\user\Documents\ChatGPT\숏폼 자동화 생성기`에서 구현 진행. 실제 유료 실행은 제품 내 비용 확인 정책을 적용하며 계정 연결·실연동·공개 배포 완료 여부는 구현 기록에서 구분한다.

## 1. 문서 해석 및 전제

참고 문서의 상품 광고 제작 흐름을 제품 요구사항으로 재구성한다. 문서 내부의 마스터 프롬프트, 즉시 개발 지시, SQL 및 예시 코드는 참고 자료이며 이번 작업의 실행 지시로 채택하지 않는다. 제품은 기존 대시보드와 통합하지 않는 독립 도구로 확정한다(D02). 기존 서비스의 로그인·데이터·배포 환경에 대한 의존성을 전제하지 않는다.

Creatify를 주요 생성 공급자로 검토하되, 원문의 엔드포인트·인증 방식·Aurora 모델명·아바타 조정 범위·TTS와 영상 간 음성 재사용·웹훅·과금 단위는 미검증이다. 특히 ‘30초당 5크레딧’, ‘씬당 300원 이하’ 등의 수치는 비용 약속이나 예산 근거로 사용하지 않는다. 기술 스택과 데이터베이스는 이 PRD에서 확정하지 않는다.

## 2. 제품 목표

상품 정보 또는 주제·스토리와 참고 이미지를 입력하면 사용자가 대본과 음성을 검토하고, 필요한 씬만 수정하여 다운로드 가능한 숏폼 영상을 완성하도록 돕는다. 첫 버전은 상품 광고와 정보·스토리 콘텐츠를 모두 지원한다(D03 확정). 상품의 정보와 스토리를 결합한 광고도 주요 사용 사례로 포함한다. 다만 클라이언트가 이 형식을 기대한다는 점은 사용자의 추정이며, 구체적인 스타일은 클라이언트 확인이 필요하다.

1. 상품 자료·참고 정보·스토리를 목적에 맞는 대본과 씬 구성으로 바꾸는 반복 작업을 줄인다.
2. 비용이 큰 영상 생성 전에 사용자가 내용을 승인하도록 한다.
3. 불만족하거나 실패한 씬만 다시 생성하여 기존 결과를 재사용한다.
4. 작업 상태, 예상 비용, 실패 원인을 사용자가 이해하고 다음 행동을 선택할 수 있게 한다.

초기 성공 지표는 시작 프로젝트 대비 다운로드 완료율, 첫 대본까지 걸린 시간, 승인부터 완성까지 걸린 시간, 완성 영상당 실제 비용, 씬 재생성률, 실패 후 복구율이다. 지표는 프로젝트·작업 기록으로 측정하며 목표 수치는 파일럿과 예산 결정 후 확정한다. 광고 전환율 향상을 보장하지 않는다.

## 3. 주요 사용자와 사용 상황

| 사용자 | 해결하려는 문제 | 초기 범위 |
|---|---|---|
| 의뢰한 클라이언트 1명 | 직접 자료를 입력하고 숏폼을 생성·검토·다운로드 | 유일한 제작 사용자; 상품 광고와 정보·스토리 콘텐츠 모두 지원, 구체적인 업종은 미확정 |
| 개발자(운영·유지보수 담당자) | 실패 작업 확인, 비용 추적, 외부 API 변경 대응, 결과 삭제와 복구 지원 | 납품 후 유지보수 담당으로 확정(D12); 구체적인 계약 범위·비용·지원 시간은 별도 합의 |

첫 버전은 의뢰한 클라이언트 1명이 사용하는 독립 전용 웹서비스다(D01·D02·D13 확정). 공개 회원가입과 팀 협업은 첫 버전 범위에서 제외한다. P0 출력은 한국어·세로형(9:16)·목표 30초로 확정한다(D05). 향후 60초, 다국어·다중 화면 비율로 확장할 가능성을 설계에 반영한다.

P0 콘텐츠 구성은 다음 세 가지를 제안한다. 상품 광고와 정보·스토리를 모두 지원하는 범위는 확정이며, 아래 분류와 화면 구성은 설계 제안이다.

| 구성 | 예시 | 대본 구성 제안 |
|---|---|---|
| 상품 설명형 광고 | 상품 특징·사용법·혜택 소개 | 관심 유도 → 특징·근거 → 사용 가치 → CTA |
| 정보·스토리형 콘텐츠 | 주제 설명, 브랜드 탄생 이야기, 상황극 | 궁금증·상황 → 정보·사건 전개 → 정리; 상품·구매 CTA는 선택 |
| 상품 스토리형 광고 | 일상 문제를 겪는 상황에서 상품의 쓰임을 소개 | 상황·갈등 → 상품 등장 → 검증된 특징과 연결 → CTA |

상품 스토리형 광고에서 AI가 만든 상황은 연출로 구분하고, 실제 고객 후기·실제 사건·검증된 효과처럼 제시하지 않는다. 브랜드 역사나 실제 사례를 사용할 때는 제공된 근거를 확인한다.

## 4. 핵심 사용자 흐름

### 정상 흐름

대본·음성·씬별 영상을 사용자가 검토·승인하는 방식을 확정한다(D09). 대본 승인 후 음성 생성, 오디오 콘티 승인 후 영상 생성, 합성에 사용할 모든 씬 영상 승인 후 최종 합성 순서로 진행한다. 생성 성공 상태만으로 승인 처리하지 않는다. 각 유료 실행은 D06의 비용 안내·실행 확인·한도 검사를 함께 적용한다.

1. **프로젝트 생성·자료 입력**: 제작 목적과 콘텐츠 구성을 선택하고 타깃·핵심 메시지·참고 자료를 직접 입력한다. 광고는 상품명·설명·소구점·상품 이미지·CTA를, 정보·스토리는 주제·전달할 정보 또는 줄거리·참고 이미지를 입력한다. 정보·스토리에서는 상품과 구매 CTA를 필수로 요구하지 않는다. 상품 스토리형 광고는 상품 정보와 상황·이야기를 함께 받는다. 가격·혜택은 선택 입력한다. P0 입력 방식은 직접 텍스트 입력과 이미지 업로드로 확정한다(D04). 출처 URL은 선택적으로 기록하며 페이지 내용을 자동 수집하지 않는다.
2. **AI 스토리보드 생성**: 콘텐츠 목적에 맞는 구조로 씬 초안을 만든다. 상품 스토리형 광고는 이야기 속 문제와 상품의 실제 특징을 연결한다. 정보형에는 구매 CTA를 강제하지 않는다. 사실 주장에는 참고 자료를 연결하고, 근거가 없으면 후기·수치·효능·실제 사건을 창작하지 않고 생략하거나 확인을 요청한다. 연출된 상황은 사실 자료와 구분하여 검토할 수 있게 한다.
3. **스토리보드 수정·승인**: 씬 순서, 대사, 비주얼 설명, 예상 길이를 확인·수정하고 특정 버전을 승인한다. 초기에는 자동 구성과 기본 순서 변경을 제공하고 정교한 편집은 후속으로 둔다.
4. **오디오 콘티 검토·승인**: 씬별 음성을 미리 듣고 실제 길이를 확인한다. 이 단계의 콘티는 씬 설명·참조 이미지·오디오의 조합이며 별도의 AI 콘티 이미지 생성은 포함하지 않는다. 변경된 대사는 음성을 다시 생성하고 승인한다.
5. **비용 확인·씬별 영상 생성**: 생성 대상과 예상 비용 또는 과금 단위, 한도를 확인한 뒤 실행한다. 각 씬의 진행·성공·실패 상태를 표시한다. 사용자는 결과를 검토하고 필요한 씬만 수정·재생성한 뒤 사용할 버전을 선택·승인한다.
6. **합성·검수·다운로드**: 승인된 씬만 순서대로 합성하고 자막, 선택 BGM, 설정된 CTA 또는 마무리를 적용한다. 정보·스토리 콘텐츠는 CTA를 생략할 수 있다. 최종 미리보기에서 확인 후 MP4를 다운로드한다.

### 예외 및 복구 흐름

- 입력 부족: 누락된 필드를 안내하고 생성을 시작하지 않는다.
- 음성이 씬 길이를 초과: 대사 축약 또는 길이 조정 후 다시 승인한다. 음성을 임의로 자르지 않는다.
- 일부 씬 실패: 성공한 씬은 보존하고 실패한 씬만 복구 대상으로 안내한다.
- 공급자 요청 결과 불명: ‘상태 확인 중’으로 두고 기존 작업을 조회한다. 새 유료 요청을 무조건 반복하지 않는다.
- 생성 후 대사·아바타·음성·비주얼 변경: 영향받는 음성·영상의 승인을 해제한다. 기존 결과를 보존하되 최신 결과로 오인하지 않게 한다.
- 씬 순서·자막 스타일·BGM만 변경: 재사용 가능한 영상은 유지하고 최종 합성본을 새 버전으로 만든다.
- 새로고침·재접속: 저장된 프로젝트와 실행 중 작업을 복원한다.
- 합성 실패: 승인된 씬을 다시 생성하지 않고 합성만 재시도한다.

## 5. 기능 우선순위

P0는 첫 사용자가 실제 입력부터 MP4 다운로드까지 완료하는 데 필요한 범위다. P1은 P0 안정화 이후, P2는 별도 검증 후 확장한다. 아래 우선순위는 추천안이다.

### P0 — 첫 사용 가능한 버전

| ID | 기능 | 요구사항 |
|---|---|---|
| F01 | 이메일 링크 로그인·프로젝트 저장 | 지정된 클라이언트 이메일에 발송한 일회용 링크로 로그인(D15); 허용 이메일 목록 적용, 작업 목록·상세·상태 저장·복원; 운영 접근은 별도 권한으로 제한하고 공개 회원가입·비밀번호 로그인은 제공하지 않음 |
| F01a | 전용 웹서비스 제공 | 클라이언트가 별도 설치 없이 지원 브라우저에서 전용 주소로 접근(D13); HTTPS 적용, 모바일·데스크톱 중 지원 화면 범위는 출시 전 정의; 공개 검색 노출 방지와 로그인 적용 |
| F02 | 콘텐츠 목적·직접 입력·이미지 업로드 | 상품·주제·스토리 텍스트 직접 입력과 이미지 업로드(D04 확정); 유형별 필수 정보 검증, 상품과 스토리 결합 입력, 선택적 출처 URL 기록, 업로드 형식·크기 제한과 오류 안내; URL 자동 수집 제외 |
| F03 | 목적별 스토리보드 초안·편집 | 상품 광고·정보/스토리·상품 스토리형 광고 지원; 씬 대사·비주얼·순서·예상 길이 편집, 사실과 연출 구분, 근거 확인; 버전별 대본 승인 필수(D09) |
| F04 | 전체 아바타 카탈로그·음성 선택 | 공급자 API가 연결 계정에 제공하는 전체 기본 아바타를 탐색·선택(D08); 검색·페이지 탐색·미리보기, 지원 정보 표시, 한국어·9:16 및 계정 권한에 따른 사용 가능 여부 확인; 음성은 검증된 한국어 옵션을 우선 제공하며 맞춤 아바타 제작은 제외 |
| F05 | 씬별 음성 미리듣기 | 실제 재생 길이 표시, 대사 수정 후 재생성; 영상 생성 전 해당 버전의 오디오 콘티 승인 필수(D09) |
| F06 | 비용 안내·생성 한도 | 도구에서 발생한 대본·음성·영상 생성과 재생성 비용(D06c)에 프로젝트별 10,000원 한도(D06a)와 전체 프로젝트 합산 월간 100,000원 총한도(D06b)를 함께 적용; 과금 작업별 안내, 생성·재생성 실행 확인, 양쪽 잔여 예산·사용 기록 표시; 서버·저장소 고정비는 별도 관리 |
| F07 | 비동기 씬 생성 | 씬별 상태 확인, 부분 실패 격리, 재접속 복원, 중복 요청 방지 |
| F08 | 개별 씬 재생성·선택 | 특정 씬만 재생성하고 사용할 결과 버전을 명시적으로 승인; 합성에는 승인된 씬 결과만 사용(D09) |
| F09 | 최종 합성 | 씬 순서 반영, 한국어 번인 자막, 콘텐츠 목적에 따른 CTA 카드 적용·생략, 선택 BGM 및 볼륨 조절 |
| F10 | 최종 미리보기·다운로드 | 한국어·9:16·목표 30초(D05), MP4 다운로드 제공(D10) 확정; 1080×1920 및 H.264/AAC는 세부 규격 제안; 외부 공유 링크와 SNS 직접 게시 제외 |
| F11 | 최소 운영 기능 | 작업·비용·오류 조회, 안전한 재시도, 프로젝트 삭제; 업로드 원본·중간 결과·완성 영상 30일 보관(D07), 만료일·다운로드 안내 및 만료 파일 정리 |

P0 목표 출력 길이는 총 30초로 확정한다(D05). CTA 사용 시 마지막 3초를 전체 길이에 포함하고 미사용 시 본문과 마무리에 배분하는 것을 제안한다. 음성 길이에 따른 허용 오차 ±2초, 해상도·코덱은 세부 규격 제안이며 아직 사용자 확정 사항이 아니다. 아바타 생성과 출력 규격의 공급자 지원 여부는 착수 전 검증한다. BGM은 권리가 확인된 소수 프리셋 또는 미사용으로 제한한다.

### 확장을 고려한 P0 설계 요구사항

- 아바타를 소수 목록으로 고정하지 않고 공급자 카탈로그를 조회·갱신하도록 설계한다(D08). 전체 카탈로그는 연결 계정과 공식 API에서 제공되는 범위이며, 공급자 웹 앱의 모든 아바타가 API에 노출된다고 가정하지 않는다. 검색·필터는 실제 제공되는 이름·특성·지원 정보를 사용하고 알 수 없는 정보는 미확인으로 표시한다.
- 아바타 미리보기는 공급자가 제공하는 이미지·샘플을 우선 사용한다. 추가 유료 샘플 생성이 필요하면 비용 확인과 기존 한도를 적용한다. 프로젝트에 저장된 아바타가 중단되거나 권한을 잃으면 기존 결과는 보관 정책대로 유지하고 신규 생성 전에 대체 아바타를 선택·재승인하도록 안내한다.
- 언어, 화면 비율, 목표 길이를 서로 독립된 프로젝트 설정으로 보존한다. P0 화면에서는 한국어·9:16·30초만 제공한다.
- 대본 구성과 씬 수·길이는 목표 길이를 기준으로 정하도록 설계한다. 향후 60초를 추가할 때 기존 30초 프로젝트의 승인 내용과 결과가 바뀌지 않아야 한다.
- 언어별 대본·음성·자막 설정과 화면 비율별 배치 기준을 추가할 수 있도록 분리한다. 특정 한국어 음성이나 세로형 자막 위치를 모든 출력의 공통 전제로 삼지 않는다.
- 생성 공급자의 지원 언어·비율·길이를 확인하여 사용 가능한 옵션만 제공한다. 확장 가능성을 실제 공급자 지원 보장으로 간주하지 않는다.
- 향후 동일 기획에서 파생된 다른 언어·비율·길이의 결과는 원본 프로젝트·기획 버전과 연결하되 독립된 결과 버전으로 관리하도록 설계한다. 대본·음성·영상 재생성이 필요한 변경은 비용 재산정과 재승인을 거친다.
- 60초 생성, 번역, 추가 음성, 다중 비율 재배치 기능 자체는 이번 P0에 포함하지 않는다. 후속 착수 시 범위·비용·품질 기준을 별도로 확정한다.

### P1 — 반복 제작 효율 개선

- 상품 URL 자동 수집, 추출 결과 확인·수정, 수집 실패 시 수동 입력 전환. D04에 따라 P0에서 제외하며 후속 도입 여부는 별도 결정한다.
- 60초·16:9·1:1 등 추가 길이와 비율, 규격별 재구성(D05에서 확장 가능성 반영 요청; 도입 시점은 미확정).
- 브랜드 로고·색상·CTA·자막 프리셋 저장과 프로젝트 복제.
- 상세 자막 타이밍 수정, 음성 속도·발음 제어 등 검증된 옵션 확대.
- 여러 광고 후킹·대본 후보 비교, 씬 및 결과 이력 비교.
- 비용·처리 시간 분석과 완료 알림. 알림 채널은 별도 결정.

### P2 — 사업·협업 확장

아래 중 팀 협업·외부 고객 서비스화는 현재 납품 범위가 아니며, 사용자 확대를 별도로 결정할 때만 재검토한다.

- 팀·브랜드별 공간, 역할별 권한, 검수 담당자 승인.
- 다상품 일괄 생성, 다국어, 복수 생성 공급자 선택.
- 광고·SNS 계정 연결과 사용자 승인 후 게시 또는 예약 발행.
- 광고 성과 연계, 소재별 실험, 성과에 따른 후속 제작 제안.
- 외부 고객 가입, 구독·결제·사용량 청구.

## 6. 데이터 항목

아래는 논리적 데이터 요구사항이며 SQL 스키마 확정안이 아니다. 원문의 세션·에셋·스토리보드·생성 씬·완성 영상 구분에 승인·버전·비용·작업 추적을 추가한다.

| 데이터 | 주요 항목 | 목적·제약 |
|---|---|---|
| 사용자·접근 권한 | 사용자 ID, 로그인 식별자, 역할, 활성 여부 | 소유권 검증; 인증 비밀값을 평문 저장하지 않음 |
| 프로젝트 | ID, 소유자 ID, 제목, 상태, 언어, 비율, 목표 길이, 출력 설정 버전, 원본 프로젝트·기획 버전 참조(선택), 생성·수정·삭제 시각 | 사용자별 작업 복원; 언어·비율·길이를 독립 설정으로 저장하고 향후 파생 결과 추적 가능 |
| 콘텐츠 기획·참고 자료 | 제작 목적, 콘텐츠 구성, 주제, 핵심 메시지, 타깃, 줄거리·상황, 톤, 사실·연출 구분, 근거 텍스트·출처, 사용자 확인 여부, CTA 사용 여부 | 상품 광고·정보/스토리·상품 스토리형 광고 지원; 상품 연결은 비광고 콘텐츠에서 선택 |
| 상품 정보 | 상품명, 설명, 타깃, 소구점, 가격·혜택, CTA, 출처 URL, 사실 확인 여부 | 가격·혜택은 선택; 출처와 사용자 확인 기록 |
| 에셋 | ID, 프로젝트 ID, 유형, 저장 키, 원본 출처, MIME, 크기, 길이·해상도, 권리 확인, 만료 시각 | 원본 URL과 내부 저장 위치 구분 |
| 스토리보드·씬 | 버전 ID, 씬 ID, 순서, 대사, 비주얼 설명, 참조 에셋, 목표 길이, 아바타·음성 ID | 씬 ID를 순서와 분리하여 재정렬·재생성 추적 |
| 아바타 카탈로그·선택 정보 | 공급자, 아바타 ID·이름, 미리보기 주소, 제공된 특성, 지원 언어·비율·음성 조합, 계정 사용 가능 여부, 마지막 조회 시각, 선택 시점 설정 | 전체 카탈로그 탐색과 지원 차이 처리; 미제공 정보는 미확인으로 구분, 생성 직전 사용 가능 여부 재확인 |
| 승인 | 대상 종류·ID·버전, 승인 사용자, 승인 시각, 무효화 이유 | 승인된 버전과 실제 생성 입력의 일치 보장 |
| 음성 결과 | 씬·대사 버전, 공급자·음성 설정, 파일 키, 실제 길이, 상태, 생성 시각 | 대사 수정 시 이전 음성을 최신으로 사용하지 않음 |
| 생성 작업·시도 | 작업 ID, 대상 버전, 종류, 공급자 작업 ID, 중복 방지 키, 상태, 시도 횟수, 시작·종료·마지막 확인 시각, 오류 코드 | 재시도와 최초 요청을 구분; 결과 불명 상태 보존 |
| 씬 영상 결과 | 결과 ID, 씬·입력 버전, 작업 ID, 파일 키, 길이·해상도, 사용자 선택·승인 여부 | 이전 결과 보존, 합성에 사용한 버전 추적 |
| 합성·최종 영상 | 버전 ID, 사용한 씬 결과 목록·순서, 자막·타임코드, BGM·볼륨, CTA, 규격, 파일 키, 상태 | 완성본 재현과 재합성 |
| 비용 기록 | 프로젝트·작업 ID, 단계, 예상량·실제량, 단위·통화, 요율 기준 시각, 상태, 한도 포함 여부, 비용 확인 시각 | 대본·TTS·영상·재생성 비용은 한도에 포함; 합성 인프라·저장 등 운영 비용과 구분, 서버·저장소 고정비는 별도 관리; 미확인 생성 비용을 0으로 취급하지 않음 |
| 예산 설정·예약 | 프로젝트 한도, 클라이언트 월간 총한도, 예산 통화, 월 집계 기간·시간대, 작업별 예약액, 실제 확정액, 예약 해제 상태, 설정 변경자·시각 | 두 한도 모두에서 실행 가능해야 신규 과금 작업 접수; 동일 작업의 예약액과 확정액을 이중 합산하지 않음 |
| 운영 기록 | 수행자, 작업·프로젝트 ID, 이벤트 유형, 시각, 오류 요약, 삭제 상태 | 문제 추적; 비밀키와 원문 개인정보 기록 금지 |

작업 상태는 대기·실행 중·상태 확인 중·성공·실패로 구분한다. 프로젝트 단계와 개별 작업 상태는 별도로 관리하며, 공급자 상태 문자열을 사용자 상태로 직접 사용하지 않는다.

## 7. 완료 조건 및 검증 시나리오

### P0 기능 완료 조건

| ID | 검증 상황 | 통과 기준 |
|---|---|---|
| AC01 | 정상 제작 | 상품 설명형 광고·상품 없는 정보/스토리 콘텐츠·상품 스토리형 광고 각각에서 자료 입력부터 대본·음성 승인, 씬 생성·승인, 합성, 다운로드까지 실제 연동으로 완주 |
| AC02 | 승인 전 실행 시도 | 승인되지 않은 버전으로 영상 생성을 시작할 수 없고 필요한 승인 단계 안내 |
| AC03 | 생성 뒤 대사 수정 | 해당 씬의 음성·영상 승인이 해제되고 새 승인 없이는 최신 합성본에 포함되지 않음 |
| AC04 | 씬 하나 재생성 | 지정 씬에만 새 생성 요청; 다른 씬 결과와 승인 상태 유지 |
| AC05 | 중복 클릭·응답 지연 | 동일한 논리 작업을 중복 접수하지 않고 결과 불명 요청은 조회 후 처리 |
| AC06 | 공급자 오류·부분 실패 | 오류와 다음 행동 표시; 성공 씬 보존; 실패 씬 복구 가능 |
| AC07 | 브라우저 재접속 | 입력·승인 버전·결과·진행 상태가 복원됨 |
| AC08 | 합성 품질 | 승인 순서와 선택 결과 일치, 검은 공백·깨진 한글·음성 잘림 없음, 자막과 음성 시차 0.5초 이내 제안 |
| AC09 | 출력 파일 | 한국어·9:16·목표 30초 영상 다운로드·재생 가능; 해상도 1080×1920, 허용 오차 ±2초 및 CTA 길이 포함 방식은 세부 제안 확정 후 검증 |
| AC10 | 이중 한도 적용 | 프로젝트 누적 비용 또는 전체 프로젝트의 월 누적 비용에 진행 중 예약액과 새 작업 예상액을 더해 어느 한도라도 초과하면 새 유료 작업 차단; 동시 요청도 두 한도를 함께 검사·예약; 한쪽만 초과하는 경우를 각각 검증하며 기존 작업 조회·완성본 다운로드는 가능 |
| AC11 | 접근·삭제·보관 만료 | 권한 없는 프로젝트·파일 접근 차단; 삭제 프로젝트 접근 즉시 차단; 확정된 30일 보관 기간과 명시된 기산 기준으로 파일 만료일 표시·만료 처리 검증; 만료 파일의 재생·다운로드·재합성을 막고 재업로드 또는 재생성 필요 안내 |
| AC12 | 합성 실패 복구 | 영상 재생성 없이 저장된 승인 결과로 합성 재시도 성공 |
| AC13 | 단일 클라이언트 접근 | 지정된 클라이언트가 제작 흐름을 이용할 수 있고 미허용 사용자는 접근 불가; 공개 가입 경로가 없으며 운영 권한은 제작 권한과 구분 |
| AC14 | 독립 사용 | 기존 대시보드의 로그인·데이터·실행 상태에 의존하지 않고 본 도구에서 프로젝트 생성부터 다운로드까지 완료 가능 |
| AC15 | 목적별 대본과 사실 검토 | 정보 콘텐츠에 상품·구매 CTA를 강제하지 않음; 상품 스토리형 광고에서 제공된 상품 특징과 이야기 연결 확인; 근거 없는 후기·효능·실제 사건을 사실로 승인하지 않도록 확인 또는 수정 요구 |

### 출시 판단 조건

- D15 인증 검증: 허용된 이메일에는 일회용 로그인 링크를 발급하고 미등록 이메일에는 계정 존재 여부를 드러내지 않는 동일한 응답을 표시한다. 링크는 HTTPS에서만 작동하고 한 번 사용하거나 만료되면 재사용할 수 없어야 한다. 만료 시간은 15분을 초기 기준으로 두되 인증 공급자 지원 범위에서 확정한다. 로그인 시도 제한, 세션 만료·로그아웃, 이메일 접근 상실 시 운영자가 본인 확인 후 허용 이메일을 교체하는 절차를 검증한다.
- D14 소유·인수 검증: 운영 환경의 서버·파일 저장소·도메인 계정과 결제 수단이 클라이언트 명의 또는 통제 아래 있음을 확인한다. 개발자에게 별도 유지보수 권한을 부여하고 클라이언트 계정 비밀번호·결제 수단 공유를 피한다. 권한 목록, 비용 확인 경로, 백업·복구 절차, 유지보수 종료 시 권한 회수 절차를 인수 문서에 기록한다.
- D13 배포 검증: 개발자 PC나 클라이언트 PC가 꺼져 있어도 클라이언트가 지원 브라우저에서 HTTPS 주소로 접속하여 저장된 프로젝트를 복원하고 제작 흐름을 이용할 수 있어야 한다. 배포 환경에서 장시간 영상 작업, 파일 업로드·다운로드, 비동기 상태 복원을 실제로 검증한다.
- D12 운영 인수 검증: 개발자의 운영 접근 권한, 장애 접수 채널, 로그·비용 조회, 안전한 재시도, 백업·삭제 업무를 문서화한다. 정기 업데이트·외부 API 변경 대응·지원 시간·비용·종료 및 인계 조건은 유지보수 계약에서 확정한다. ‘개발자 유지보수’ 선택만으로 무제한·무상·24시간 지원을 약속하지 않는다.
- D11 납품 검증: 실제 사용하는 Creatify·대본 생성 AI 계정의 소유권과 결제 관리 권한이 클라이언트에게 있음을 확인한다. 전용 API 키 등록·교체·연결 확인 절차를 인수 문서에 포함하고, 개발자 개인 계정·결제 수단에 의존하지 않는 구성을 확인한다. 계정 소유 결정은 현재 유료 가입·키 발급·API 실행을 지시하는 것이 아니다.
- D10 전달 검증: 허용된 클라이언트가 완성본을 미리 보고 MP4 파일로 내려받아 도구 밖에서 재생할 수 있어야 한다. SNS 계정 연결이나 게시 절차 없이 다운로드를 완료한다. 다운로드용 임시 접근 주소는 내부 전달 수단이며 외부 공유 링크 기능으로 제공하지 않는다.
- D09 승인 검증: 미승인 대본으로 음성 생성, 미승인 음성으로 영상 생성, 미승인 씬으로 최종 합성이 진행되지 않음을 확인한다. 내용 수정 시 영향받는 승인·결과만 무효화하고 변경 없는 씬의 승인은 유지한다. 재생성 결과는 성공했더라도 사용자가 새로 승인해야 한다.
- D08 카탈로그 검증: 제공 목록의 모든 페이지 탐색·검색과 샘플 미리보기를 확인한다. 한국어·9:16 조건에 맞는 아바타 선택·생성을 검증하고, 사용 불가·지원 미확인·목록 조회 실패·선택 후 제공 중단 시 안내와 복구를 확인한다. 모든 아바타의 품질을 동일하게 보장하는 조건은 아니다.
- D05 확장 준비 검토: 언어·비율·길이의 독립 설정, 목표 길이 기반 씬 구성, 언어·화면 배치 설정의 분리, 기존 프로젝트·승인 버전 보존을 설계 검토로 확인한다. P0 완료를 위해 B·C 규격의 실제 생성까지 요구하지 않는다.
- D01~D15의 P0 관련 결정을 반영하고, 필수 공급자 기능과 과금 조건을 공식 자료 및 승인된 연동 검증으로 확인한다.
- 특히 승인된 TTS 음성을 영상 생성에 재사용할 수 있는지 확인한다. 불가능하면 오디오 프리뷰와 최종 음성 일치 조건을 재설계하고 사용자에게 변경안을 확인받는다.
- 정상 제작·부분 실패·중복 요청·버전 변경·접근 제어 시나리오를 모두 통과한다. 모의 생성만으로 실제 연동 완료를 판정하지 않는다.
- 외부 서비스 장애, 비용 한도 초과, 결과 불명 작업의 운영 대응 절차와 담당자를 정한다.
- 제작 시간·성공률·비용 목표 수치는 D06과 파일럿 결과로 정한다. 현재 숫자를 임의의 출시 보장으로 확정하지 않는다.
- 이 문서 작성은 위 기능 구현 완료를 의미하지 않는다.

## 8. 제외 범위

P0에서 제외하는 항목:

- 60초·다국어·추가 화면 비율의 실제 제작 기능. D05에 따른 확장 준비 설계는 P0에 포함한다.
- 외부 공유 링크 생성과 SNS·광고 플랫폼 직접 게시(D10에 따라 P0 제외), 광고 집행·예산 운용, 성과 최적화. 내려받은 MP4의 전달·게시는 클라이언트가 수행한다.
- 공개 회원가입, 결제·구독, 고객별 요금제와 청구.
- 팀 협업, 복잡한 권한 체계, 여러 브랜드 동시 관리.
- URL 자동 크롤링, 로그인·유료·접근 제한 페이지 수집.
- 대량 제작, 완전 무인 자동 생성, 승인 없는 자동 유료 재생성.
- 실제 인물의 얼굴·음성 복제, 커스텀 아바타 학습, 세부 의상·동작 변경 보장.
- 전문 영상 편집기의 자유로운 타임라인·키프레임 편집.
- AI가 상품의 실제 성능·법적 적합성·광고 성과를 보증하는 기능.
- 기존 대시보드 수정·통합·배포 및 기존 서비스 데이터 이관(D02 독립 도구 선택에 따라 제외).

## 9. 보안·운영 가정

### 보안 및 콘텐츠

- 클라이언트 1명에게 접근이 제한된 전용 도구로 제공한다(D01 확정). Creatify와 대본 생성 AI의 계정·API 키 통제권은 클라이언트가 보유하고 공급자 사용료를 직접 결제한다(D11 확정). 납품 후 개발자가 운영·유지보수를 담당한다(D12 확정).
- 제작 사용자 로그인은 사전에 등록된 클라이언트 이메일로 보내는 일회용 링크를 사용한다(D15 확정). 공개 가입과 임의 이메일 등록은 제공하지 않는다. 링크 토큰은 원문을 저장·기록하지 않고 짧은 만료 시간과 일회성 사용을 적용하며, 로그인 요청에는 속도 제한을 둔다. 인증 이메일 발송 서비스의 계정 소유·비용은 클라이언트 소유 인프라 원칙(D14)을 따른다.
- 운영·유지보수 접근은 클라이언트가 허용한 최소 범위로 제한한다. 클라이언트의 계정 비밀번호를 공유받는 대신 공급자가 지원하는 위임 권한이나 도구 전용 API 키를 사용한다. 지원 종료 시 운영 권한 회수·키 교체가 가능하도록 인수 절차에 포함한다.
- 개발자는 합의된 유지보수 범위에서 장애 확인·복구, 외부 API 변경 대응, 보안 업데이트와 운영 지원을 수행한다(D12). 기능 추가는 유지보수와 구분하여 별도 범위·일정을 합의한다. 클라이언트 데이터 열람은 지원에 필요한 최소 범위로 제한하고 운영 행위를 기록한다.
- API 비밀키는 서버 측 비밀 저장소에서 관리하고 브라우저·저장소·로그에 노출하지 않는다.
- 프로젝트와 파일은 소유권을 검사한다. 비공개 파일과 만료되는 다운로드 링크를 사용하며 외부 공급자 원본 URL을 영구 배포 링크로 취급하지 않는다.
- 파일은 허용 형식·실제 파일 유형·크기를 검증한다. P0 제안 제한은 상품 이미지 JPEG/PNG/WebP, 파일당 10MB, 프로젝트당 10개다.
- 상품 자료와 향후 수집 페이지에 담긴 명령문을 시스템 지시로 실행하지 않는다. URL 수집 도입 시 내부 네트워크 접근·위험한 리디렉션을 차단한다.
- 사용자는 이미지·로고·BGM·상품 문구의 사용 권한을 확인한다. 근거 없는 효능·후기·가격을 만들어 넣지 않으며 광고 공개 전 검토 책임을 명시한다.
- 외부 공급자에게 전달하는 상품 자료·대사·이미지의 범위를 안내한다. 공급자의 보존·학습 이용·처리 지역 조건은 미확정이며 민감정보는 입력하지 않는 것을 초기 가정으로 둔다.

### 운영 및 비용

- 기존 서비스와 독립된 클라이언트 전용 웹서비스로 제공한다(D02·D13 확정). 클라이언트는 지원 브라우저로 전용 주소에 접근하며, 개발자·클라이언트 개인 PC의 상시 가동에 의존하지 않는다. 서버·파일 저장소·도메인 계정은 클라이언트가 소유하고 고정 운영비를 직접 결제하며, 개발자는 별도 권한으로 유지보수한다(D14 확정). 로그인 방식은 D15에서 결정한다.
- 배포 환경은 HTTPS, 비공개 파일 접근, 장시간 비동기 작업, 30일 보관과 MP4 다운로드를 지원해야 한다. 호스팅 제품과 기술 스택은 이 PRD에서 아직 확정하지 않는다.
- 긴 영상 작업은 비동기로 처리하고, 외부 서비스 제한에 맞춰 동시 실행 수를 제한한다. 초기 제안은 사용자당 실행 프로젝트 1개, 씬 동시 생성 최대 2개이며 공급자 제한에 따라 낮춘다.
- 생성 요청 타임아웃은 실패 확정과 구분한다. 폴링·웹훅 중 실제 지원되는 방식으로 기존 작업의 종료 여부를 확인한다.
- 상태 조회·일시 오류 복구와 새 유료 생성을 구분한다. 새 유료 생성은 사용자 실행 확인을 받고, 자동 재시도는 추가 과금·중복 생성이 없음을 확인한 경우로 제한한다.
- 예상 비용에는 진행 중 작업의 예약 비용을 포함한다. 실제 비용 확인이 늦을 수 있음을 표시하고 가격·크레딧 환산을 공급자 기준으로 관리한다. 실행 후 취소·환불 가능 여부는 보장하지 않는다.
- 프로젝트별 누적 한도와 클라이언트 전체의 월간 총한도를 동시에 적용한다(D06 확정). 최초 생성·음성 생성·유료 재생성 비용을 합산하며 한도에 도달하면 새 과금 작업을 막는다. 진행 중 작업을 자동 취소하거나 기존 결과를 삭제하지 않는다. 실패했더라도 공급자 과금 여부가 확인되기 전에는 예약 비용을 임의로 해제하지 않는다.
- 프로젝트 예산은 프로젝트 전체 수명 동안 누적하고, 월간 예산은 한국시간의 달력 월 기준으로 집계하는 것을 제안한다. 월말 진행 중 작업의 예약액은 요청 월에 유지하고 실제 비용 확인 시 해당 월에 정산하여 월 변경으로 예약이 소실되지 않게 한다. 이는 공급자 청구서의 집계 시점과 다를 수 있다.
- 프로젝트별 생성 예산은 원화 10,000원으로 확정한다(D06a). 최초 생성·음성 생성·재생성을 프로젝트 전체에 걸쳐 누적한다. 예를 들어 확정 비용 7,000원과 진행 중 예약액 2,000원이 있다면 예상액 1,500원의 새 과금 작업은 접수하지 않는다. 이는 한도 검증용 예시이며 실제 공급자 단가가 아니다.
- 월간 총한도는 원화 100,000원으로 확정한다(D06b). 모든 프로젝트의 해당 월 비용을 합산하며 프로젝트 수 자체에는 10개 제한을 두지 않는다. 예를 들어 월 확정 비용 95,000원과 예약액 4,000원이 있으면 새 작업 예상액 2,000원은 프로젝트별 예산이 남아 있어도 접수하지 않는다. 이는 월간 한도 검증용 예시다.
- 생성 예산에는 본 도구에서 발생한 대본·음성·영상 생성 및 각 단계의 유료 재생성 비용을 포함한다(D06c 확정). 대본 초안·수정 요청도 유료 호출이면 한도 검사·예약·정산 대상이다. 서버·저장소 고정비는 생성 예산에서 제외하고 별도로 관리한다. 생성 비용과 운영 비용은 화면·기록에서 구분하여 월간 10만 원을 전체 서비스 운영비로 오인하지 않게 한다.
- 공급자 요율·환산 기준을 확인한 뒤 원화 예상액을 계산한다. 한도 금액은 지출 허용 범위이며 실제 제작 단가나 한도 내 완성 보장이 아니다. 공급자 계정에서 본 도구 외부로 발생한 사용액은 도구의 생성 예산과 구분한다. 본 도구의 실행 제한으로 외부 계정 전체의 청구액을 보장하지 않는다.
- 업로드 원본·중간 결과·완성 영상의 보관 기간은 30일로 확정한다(D07). 기산 기준은 파일별 업로드 또는 생성 완료 시각을 제안하며, 프로젝트를 열거나 다운로드하는 것만으로 보관 기간을 연장하지 않는다. 기산 기준은 사용자 확정 사항과 구분하여 출시 전 명시한다.
- 파일별 만료일과 다운로드 안내를 화면에 표시한다. 만료 후 파일을 정리하며, 해당 파일이 필요한 미리보기·다운로드·재합성은 이용할 수 없음을 안내한다. 다운로드한 사용자 사본에는 서비스의 30일 삭제 정책이 적용되지 않는다.
- 작업 중인 파일이 만료되는 경우 현재 작업 종료까지 최소한으로 삭제를 유예하고, 유예와 정리 결과를 기록하는 것을 제안한다. 임시 파일은 작업 종료 후 24시간 내 정리하는 것을 제안한다. 프로젝트의 텍스트·승인·비용 기록 보존 기간, 백업 정리 기간, 공급자 측 보존 정책은 파일 30일 보관과 구분하여 후속 운영 설계에서 정한다.
- 제한된 파일럿 운영을 가정하며 24시간 지원이나 처리 시간 SLA는 약속하지 않는다. 공급자 장애 시 상태 안내와 복구 수단을 제공한다.
- FFmpeg 등 서버 합성 환경, 파일 저장소, 작업 처리 환경은 후보 아키텍처다. 이 PRD에서는 설치·구축하지 않는다.

## 10. 의사 결정 목록

**D01~D15 및 D06a~D06c는 확정**이다. 생성 비용에 프로젝트별 1만 원·월간 총한도 10만 원을 적용하며, 파일은 30일 보관하고 공급자 전체 아바타 카탈로그를 제공한다. 대본·음성·씬별 영상은 단계별 사용자 승인을 거치고 완성본은 MP4 다운로드로 전달한다. API·서버·저장소 계정과 비용은 클라이언트가 통제하고, 개발자가 납품 후 유지보수하는 클라이언트 전용 웹서비스로 제공한다. 지정 이메일의 일회용 링크로 로그인한다.

| ID | 결정할 사안 | 확정된 선택 | 결정 상태 | 선택에 따른 영향 |
|---|---|---|---|---|
| D01 | 첫 버전 사용자·운영 형태 | 사용자 직접 지정: 의뢰한 클라이언트 1명 | 확정 | 단일 클라이언트 전용 제작 도구; 공개 가입·팀 협업 제외, 운영 접근은 별도 구분 |
| D02 | 기존 서비스와 관계 | A 독립 도구 선택 | 확정 | 기존 대시보드 통합 제외; 독립적인 접근·프로젝트 관리·제작 흐름 제공. 실행·배포 방식은 별도 결정 |
| D03 | 첫 영상의 콘텐츠 범위 | C 둘 다 선택; 상품 정보·스토리를 결합한 광고 포함 | 확정 | 목적별 입력·대본·검수 기준 추가; 상품 스토리형 광고에 대한 클라이언트 기대는 사용자 추정으로 기록 |
| D04 | 입력 방식 | A 직접 정보·스토리 입력 및 이미지 업로드 선택 | 확정 | P0는 직접 입력·업로드로 제작; 출처 URL은 선택적 기록만 제공하고 자동 수집은 후속 후보로 유지 |
| D05 | 첫 출력 규격 | A 한국어·9:16·30초 선택; 향후 B 30/60초·C 다국어/다중 비율 확장 고려 | 확정 | P0는 A만 제공; 언어·비율·길이를 독립 설정으로 두고 후속 확장 시 기존 프로젝트 보존. B·C 구현 시점은 미확정 |
| D06 | 유료 생성 예산 정책 | C 프로젝트별 한도+월간 총한도+실행 확인 선택 | 확정 | 개별 영상과 전체 지출을 함께 통제; 어느 한도라도 초과하면 신규 과금 작업 차단. 금액은 별도 결정 |
| D06a | 프로젝트별 생성 예산 금액 | A 1만 원 선택 | 확정 | 최초 생성·음성 생성·재생성 누적 상한 10,000원. 비용 확정액과 진행 중 예약액을 포함해 신규 작업 검사 |
| D06b | 월간 총한도 금액 | A 10만 원 선택 | 확정 | 전체 프로젝트의 월 누적 확정 비용·예약액과 새 작업 예상액을 합산하여 100,000원 초과 신규 작업 차단; 프로젝트 수 자체를 10개로 제한하지 않음 |
| D06c | 예산 산정 범위 | A 도구에서 발생한 생성 비용만 선택 | 확정 | 대본·음성·영상 생성 및 재생성 합산; 서버·저장소 고정비는 별도 관리, 월간 한도는 전체 운영비 상한이 아님 |
| D07 | 결과 보존 정책 | A 30일 선택 | 확정 | 업로드 원본·중간 결과·완성 영상 30일 보관; 만료일과 다운로드 필요 안내. 파일별 기산 기준은 제안 상태이며 텍스트·비용 기록·백업·공급자 보존은 별도 정의 |
| D08 | 아바타 범위 | B 공급자 전체 카탈로그 선택 | 확정 | 연결 계정·API에서 제공되는 전체 기본 아바타 탐색; 검색·미리보기·지원 조건·제공 중단 대응을 P0에 포함, 맞춤 아바타 제작은 제외 |
| D09 | 검토·승인 수준 | A 대본·음성·씬별 영상 승인 선택 | 확정 | 대본 승인 후 음성 생성, 음성 승인 후 영상 생성, 사용 씬 승인 후 합성; 변경 영향 범위만 재승인하며 유료 실행 확인·예산 한도 유지 |
| D10 | 배포 범위 | A MP4 다운로드 선택 | 확정 | 클라이언트가 내려받은 파일을 직접 전달·게시; 외부 공유 링크와 SNS 계정 연동·직접 게시 기능은 P0 제외 |
| D11 | 공급자 계정·사용료 부담 | A 클라이언트 소유·직접 결제 선택 | 확정 | Creatify·대본 생성 AI 계정과 결제 통제권을 클라이언트가 보유; 운영 담당자 권한은 별도 부여하며 서버·저장소 계정은 별도 결정 |
| D12 | 납품 후 운영 담당 | B 개발자 유지보수 선택 | 확정 | 개발자가 합의 범위 내 장애·외부 API 변경·업데이트 대응; 범위·비용·접근 권한·지원 시간·종료 조건은 별도 계약 필요 |
| D13 | 실행·배포 방식 | A 클라이언트 전용 웹서비스 선택 | 확정 | 브라우저 기반 전용 주소·HTTPS·로그인 필요; 개인 PC 상시 가동에 의존하지 않음; 서버·도메인·저장소 운영비 발생 |
| D14 | 서버·저장소 계정 소유·운영비 | A 클라이언트 소유·직접 결제 선택 | 확정 | 서버·파일 저장소·도메인을 클라이언트가 통제하고 개발자에게 유지보수 권한만 부여; 고정 운영비는 생성 예산과 별도 결제 |
| D15 | 로그인 방식 | A 지정 이메일 일회용 로그인 링크 선택 | 확정 | 허용 이메일만 링크 발급; 링크 일회성·단기 만료·요청 제한 적용; 이메일 접근 상실 시 운영자 본인 확인 후 주소 교체 |

### 결정 기록

| 항목 | 사용자 결정 | 반영 상태 |
|---|---|---|
| D01 | 의뢰한 클라이언트 1명이 사용 | 주요 사용자, 접근 기능, 완료 조건, 보안·운영 가정에 반영 |
| D02 | A. 독립 도구 | 제품 전제, 사용 상황, 완료 조건, 제외 범위, 운영 가정에 반영 |
| D03 | C. 상품 광고와 정보·스토리 모두; 상품 정보·스토리를 활용한 광고 포함 | 제품 목표, 사용 사례, 입력·대본 흐름, P0 기능, 데이터, 완료 조건에 반영; 클라이언트 기대에 대한 추정은 별도 표시 |
| D04 | A. 직접 입력·이미지 업로드 | 핵심 입력 흐름, P0 기능, URL 수집의 후속 범위에 반영 |
| D05 | A. 한국어·9:16·30초로 시작, 향후 B·C 확장 가능성 고려 | 출력 기능, 확장 설계 요구사항, 데이터, 완료 조건, 제외 범위에 반영 |
| D06 | C. 프로젝트별 한도와 월간 총한도 함께 적용 | P0 비용 기능, 예산 데이터, 한도 검증 조건, 운영 가정에 반영 |
| D06a | A. 프로젝트별 생성 예산 1만 원 | P0 비용 기능과 운영 한도·검증 예시에 반영 |
| D06b | A. 월간 총한도 10만 원 | P0 비용 기능, 운영 기준과 월간 한도 검증 예시에 반영 |
| D06c | A. 생성 비용만 포함 | P0 비용 기능, 비용 데이터, 대본 호출 포함 여부와 운영비 구분에 반영 |
| D07 | A. 업로드 원본·중간 결과·완성 영상 30일 보관 | 운영 기능, 만료 검증 조건, 보관·삭제 가정에 반영 |
| D08 | B. 공급자 전체 아바타 카탈로그 | P0 선택 기능, 조회·갱신 요구, 아바타 데이터, 출시 검증 조건에 반영 |
| D09 | A. 대본·음성·씬별 영상 단계별 승인 | 핵심 흐름, P0 기능, 승인·재승인 검증 조건에 반영 |
| D10 | A. MP4 다운로드 | 최종 출력 기능, 전달 완료 조건과 제외 범위에 반영 |
| D11 | A. 클라이언트 계정 소유·사용료 직접 결제 | 계정·키 관리, 납품 검증과 운영 역할 구분에 반영 |
| D12 | B. 개발자 유지보수 | 주요 사용자, 출시 조건, 보안·운영 가정에 반영; 계약 세부 조건은 별도 결정 |
| D13 | A. 클라이언트 전용 웹서비스 | P0 접근 기능, 배포 완료 조건과 운영 가정에 반영 |
| D14 | A. 클라이언트가 서버·저장소 계정 소유 및 운영비 직접 결제 | 배포 운영 가정, 접근 권한, 인수·종료 조건에 반영 |
| D15 | A. 지정 이메일 일회용 로그인 링크 | P0 접근 기능, 인증 검증, 보안·운영 가정에 반영 |

현재 PRD의 주요 제품 의사 결정 항목은 모두 답변되었다. 공급자 API 기능·과금 검증, 호스팅 제품·기술 스택, 상세 화면·스키마는 구현 전 별도 기술 설계에서 확정하며 본 PRD 결정으로 간주하지 않는다.
