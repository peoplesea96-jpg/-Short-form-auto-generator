import {z} from 'zod';
import {db,tx,id,hash,assert,getProject,saveProject,budget,month,audit} from './db.mjs';
export const briefSchema=z.object({title:z.string().trim().min(1).max(100),kind:z.enum(['product','story','hybrid']),description:z.string().trim().min(10).max(6000),audience:z.string().trim().min(1).max(300),facts:z.string().max(6000),cta:z.string().max(120),source:z.union([z.literal(''),z.url()]),rights:z.literal(true)});
export const sceneSchema=z.object({id:z.string(),narration:z.string().trim().min(1).max(500),visual:z.string().max(600),evidence:z.string().max(600),fiction:z.boolean(),seconds:z.number().min(2).max(15),avatar:z.string().max(100),voice:z.string().max(100),asset:z.string().max(100)});
export function createProject(owner,input){const brief=briefSchema.parse(input);const p={id:id(),owner,brief,revision:1,language:'ko',ratio:'9:16',duration:30,scenes:[],assets:[],finals:[],bgm:'none',bgmVolume:0.12,created:Date.now(),updated:Date.now()};db.prepare('INSERT INTO projects(id,owner,body) VALUES(?,?,?)').run(p.id,owner,JSON.stringify(p));audit(owner,'project.create',p.id);return p;}
export const active=p=>db.prepare("SELECT id FROM jobs WHERE project=? AND status IN ('queued','submitting','running','unknown')").get(p.id);
export function updateScenes(owner,pid,revision,input){return tx(()=>{const p=getProject(pid,owner);assert(p.revision===revision,'다른 변경사항이 있습니다. 새로고침해 주세요.',409);assert(!active(p),'진행 중인 작업이 끝난 뒤 수정해 주세요.');const scenes=z.array(sceneSchema).min(1).max(12).parse(input);assert(new Set(scenes.map(s=>s.id)).size===scenes.length,'씬 ID가 중복되었습니다.');assert(Math.abs(scenes.reduce((n,s)=>n+s.seconds,0)-(p.brief.cta?27:30))<=.1,'씬 길이 합계가 목표 길이와 다릅니다.');p.scenes=scenes.map(s=>{assert(!s.asset||p.assets.some(a=>a.id===s.asset),'이미지를 다시 선택해 주세요.');const old=p.scenes.find(x=>x.id===s.id);if(old&&hash(JSON.stringify(sceneSchema.parse(old)))===hash(JSON.stringify(s)))return old;return {...s,revision:(old?.revision||0)+1,scriptApproved:false,audioApproved:false,videoApproved:false,audio:null,video:null,history:old?[...(old.history||[]),...(old.video?[{...old.video,sceneRevision:old.revision}]:[])]:[]};});p.revision++;saveProject(p);audit(owner,'scenes.edit',pid);return p;});}
export function approve(owner,pid,sceneId,stage){return tx(()=>{const p=getProject(pid,owner);assert(!active(p),'진행 중인 작업을 먼저 확인해 주세요.');const targets=sceneId?p.scenes.filter(s=>s.id===sceneId):p.scenes;assert(targets.length,'씬이 없습니다.');for(const s of targets){if(stage==='script'){assert(s.narration&&s.avatar&&s.voice,'대사·아바타·음성을 선택해 주세요.');assert(s.fiction||s.evidence.trim(),'사실 근거를 입력하거나 연출임을 표시해 주세요.');s.scriptApproved=true;}else if(stage==='audio'){assert(s.scriptApproved&&s.audio,'음성을 먼저 생성해 주세요.');assert(s.audio.duration<=s.seconds+.5,'음성이 씬보다 깁니다. 대사 또는 길이를 조정해 주세요.');assert(s.audio.expires>Date.now(),'음성이 만료되었습니다.');s.audioApproved=true;}else if(stage==='video'){assert(s.audioApproved&&s.video&&s.video.expires>Date.now(),'유효한 영상을 먼저 생성해 주세요.');assert(s.video.duration<=s.seconds+.5,'영상 음성이 씬보다 깁니다. 대사와 길이를 조정해 주세요.');s.videoApproved=true;}else assert(false,'잘못된 승인 단계입니다.');}p.revision++;saveProject(p);audit(owner,`approve.${stage}`,pid);return p;});}
export function rates(){const keys=['KRW_PER_CREDIT','SCRIPT_MAX_KRW','OPENAI_INPUT_KRW_PER_MILLION','OPENAI_OUTPUT_KRW_PER_MILLION'];assert(process.env.RATES_REVIEWED_AT&&keys.every(k=>Number.isFinite(Number(process.env[k]))&&Number(process.env[k])>0),'계정별 요율 검토와 비용 설정이 필요합니다.',503);return {credit:Number(process.env.KRW_PER_CREDIT),script:Number(process.env.SCRIPT_MAX_KRW)};}
export function plan(p,kind,sceneId){assert(['script','audio','video','merge'].includes(kind),'잘못된 작업입니다.');const selected=sceneId?p.scenes.filter(s=>s.id===sceneId):p.scenes;assert(kind==='script'||selected.length,'씬이 없습니다.');if(kind==='script'){assert(!p.scenes.length,'기존 대본은 씬 편집에서 수정해 주세요.');assert(process.env.OPENAI_API_KEY&&process.env.OPENAI_MODEL,'대본 생성 API 설정이 필요합니다.',503);}if(['audio','video'].includes(kind)){assert(process.env.CREATIFY_API_ID&&process.env.CREATIFY_API_KEY,'Creatify 계정 연결이 필요합니다.',503);for(const s of selected){assert(s.scriptApproved,'대본을 먼저 승인해 주세요.');if(kind==='video')assert(s.audioApproved&&s.audio?.expires>Date.now(),'음성을 먼저 승인해 주세요.');}}if(kind==='merge'){assert(p.scenes.every(s=>s.videoApproved&&s.video?.expires>Date.now()&&(!s.asset||p.assets.some(a=>a.id===s.asset&&a.expires>Date.now()))),'모든 씬의 유효한 영상을 승인해 주세요.');return {amount:0,selected:p.scenes};}const r=rates();const model=process.env.CREATIFY_MODEL||'standard';assert(['standard','aurora_v1','aurora_v1_fast'].includes(model),'지원하지 않는 영상 모델입니다.');const inputBound=Buffer.byteLength(JSON.stringify(p.brief),'utf8')+6000;const scriptBound=Math.ceil((inputBound*Number(process.env.OPENAI_INPUT_KRW_PER_MILLION)+2400*Number(process.env.OPENAI_OUTPUT_KRW_PER_MILLION))/1e6);const amount=kind==='script'?Math.max(r.script,scriptBound):selected.reduce((sum,s)=>sum+Math.ceil((kind==='audio'?Math.ceil(Math.max(s.seconds,s.narration.length/2)/30):model==='standard'?5*Math.ceil(s.seconds/30):Math.ceil(s.seconds)*(model==='aurora_v1'?1:.5))*r.credit),0);return {amount:Math.ceil(amount),selected};}
export function quote(owner,pid,kind,scene){const p=getProject(pid,owner);const q=plan(p,kind,scene);return {amount:q.amount,revision:p.revision,kind,scene,budget:budget(owner,pid),rateDate:process.env.RATES_REVIEWED_AT};}
export function selectVideo(owner,pid,sceneId,fileId){return tx(()=>{const p=getProject(pid,owner);assert(!active(p),'진행 중인 작업이 있습니다.');const s=p.scenes.find(s=>s.id===sceneId);const old=s?.history?.find(v=>v.id===fileId);assert(old&&old.sceneRevision===s.revision&&old.expires>Date.now(),'현재 대본·음성과 일치하는 유효한 영상만 선택할 수 있습니다.');const previous=s.video;s.video=old;s.videoApproved=false;s.history=s.history.filter(v=>v.id!==fileId);if(previous)s.history.push({...previous,sceneRevision:s.revision});p.revision++;saveProject(p);audit(owner,'video.select',pid);return p;});}
export function enqueue(owner,pid,q,idem){return tx(()=>{assert(typeof idem==='string'&&idem.length>=16&&idem.length<100,'요청 ID가 필요합니다.');const prior=db.prepare('SELECT * FROM jobs WHERE idem=?').get(idem);if(prior){assert(prior.owner===owner&&prior.project===pid,'잘못된 요청 ID입니다.');return prior;}const p=getProject(pid,owner);assert(p.revision===q.revision,'내용이 변경되었습니다. 비용을 다시 확인해 주세요.',409);assert(!active(p),'이미 진행 중인 작업이 있습니다.',409);const {amount,selected}=plan(p,q.kind,q.scene);assert(amount===q.amount,'요율이 변경되었습니다. 비용을 다시 확인해 주세요.',409);const b=budget(owner,pid);assert(b.project+amount<=10000&&b.month+amount<=100000,'프로젝트 또는 월간 예산 한도를 초과합니다.');const jid=id();db.prepare('INSERT INTO jobs(id,project,owner,kind,scene,revision,input,status,reserved,month,created,updated,idem) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)').run(jid,pid,owner,q.kind,q.scene||null,p.revision,JSON.stringify({project:p,selected,model:process.env.CREATIFY_MODEL||'standard',rates:q.kind==='merge'?{}:rates()}),'queued',amount,month(),Date.now(),Date.now(),idem);audit(owner,`enqueue.${q.kind}`,pid);return db.prepare('SELECT * FROM jobs WHERE id=?').get(jid);});}
