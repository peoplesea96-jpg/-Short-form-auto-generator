# 상세페이지 이미지 분석으로 프로젝트 초안 만들기

## 사용 흐름

새 프로젝트 → 자료 사용 동의 → JPG/PNG/WebP 업로드 → 분석하고 초안 만들기 → 예상 비용 확인 → 분석 결과 검토 → 상품/옵션 선택 → 항목별 수정·선택 적용 → 프로젝트 생성.

최대 10장, 장당 10MB, 이미지당 4천만 픽셀. 긴 이미지는 겹치는 구간으로 분할하며 전체 32개 영역, 분석 요청 40MB 제한을 적용한다. 파일 순서 변경 및 삭제 후 이전 분석과 견적은 무효화한다.

원본 기재 정보와 AI 광고 제안은 구분한다. 확인 필요로 분류된 문구는 근거란에 자동 포함하지 않으며 사용자가 확인 후 추가한다. 추출 문구별 출처 번호로 이미지 구간을 확인할 수 있다. 선택한 입력란만 변경하고 기존 내용이 있으면 덮어쓰기 확인을 받는다. 영상 길이, 유형, 출처 URL은 분석으로 바꾸지 않는다.

## 보관과 비용

계정별 임시 자료를 7일 보관한다. 새 프로젝트 창에서 이전 자료를 이어서 작업할 수 있다. 프로젝트 생성 시 원본, 분석 결과, 사용자의 최종 적용 기록, 분석 작업 및 비용 기록을 같은 프로젝트로 연결한다. 원본은 별도의 상세페이지 자료이며 영상 생성용 assets에 자동 등록하지 않는다. 연결한 파일은 30일 보관한다.

기존 OPENAI_API_KEY, OPENAI_MODEL 및 입력/출력 원화 요율 설정을 사용한다. 설정 모델은 이미지 입력 및 구조화된 출력을 지원해야 한다. 분석 전에 예상 비용을 예약하고 응답 사용량으로 정산한다. 결과 불명 요청은 자동 재실행하지 않으며 운영 확인 후 재시도한다. 프로젝트를 생성하지 않아도 월간 비용에 남는다.

## 검증

- 자동 테스트 30개 통과: 긴 이미지 구간, 계정 격리, 파일 제한, 중복 실행, 오래된 견적 차단, 출처 검증, 실패 시 자료 보존, 예산, 프로젝트 연결.
- 로컬 브라우저 + 실제 서버/작업 프로세스 + 모의 AI 응답으로 전체 흐름 통과.
- 상품/옵션 전환, 기존 입력 보존, 검토 적용, 프로젝트 원본 연결, 비로그인 파일 접근 차단 확인.
- 390px 모바일 가로 넘침 없음. 검토 화면 시각 확인.
- 실제 유료 API는 호출하지 않았으므로 실제 상품 상세페이지의 추출 정확도와 운영 계정 모델 호환성은 운영 검증 대상이다.
- 운영 배포는 아직 실행하지 않음.

## 후속 범위

상품 사진 영역 선택/추출, URL 수집, PDF, 상품 소재 보관함은 후속 기능이다.

## API 형식 참고

[OpenAI 공식 이미지 입력 문서](https://developers.openai.com/api/docs/guides/images-vision)
